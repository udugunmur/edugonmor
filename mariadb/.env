# MariaDB Admin Credentials
# Usuario root (por defecto en MariaDB)
# Password se gestiona via Docker Secrets (docker/secrets/db_password.txt)

# Nexus Registry Credentials
# ============================================================================
NEXUS_USER=nexus_user
NEXUS_PASSWORD=nexus_password
NEXUS_REGISTRY=nexus.edugonmor.com/repository/docker-hosted
